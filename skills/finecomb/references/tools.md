# Part VII: Machine-checkable commands

This part is a table of tool options, not a list of things you must run. For when to run anything, see [execution boundaries during review](scope.md#execution-boundaries-during-review). Choosing the smallest entry point, reusing caches and handling failures follow the target project's test strategy.

Rows are grouped by "what to check", and each row gives common tools for each ecosystem. Before using them, do three things:

- **Replace and check the placeholders.** `<target-dir>` and `<file>` are paths within this review's scope; `<pkg>`, `<module>` and `<crate>` are specific packages or modules; `<tmp>` is a temporary directory used only by this run. Test names, budgets, platforms and arguments must all come from the actual target. Run commands in the owning module or at the project root, and take module and workspace options from the nearest testing notes.
- **Confirm the tool is already on the machine; do not install it automatically.** Commands such as `npx`, `pipx run` and `go run <module>@<version>` download over the network when the tool is missing, so handle them per authorization first.
- **Prefer tools the project has already chosen.** The ones listed are only examples; for ecosystems not listed, pick a tool with the same purpose.

## Read-only discovery

| Purpose | Example | Limits of the result |
| --- | --- | --- |
| File inventory and leftovers | `rg --files --hidden -g '!.git/**' -g '!**/.git/**' '<target-dir>'` | Lists only the given directory; ignore rules may exclude files, so empty output does not prove the directory has no content |
| Languages and size | `tokei '<target-dir>'` or `scc '<target-dir>'`; without such tools, count extensions from the previous row's output | Judges only by extension and file header, and cannot recognize languages embedded in strings; use the result for sharding and for picking [Appendix A](languages.md) |
| Identifying generated code | `rg -l -F -e 'DO NOT EDIT' -e '@generated' -e 'auto-generated' -e 'automatically generated' -- '<target-dir>'`; then read `linguist-generated` and `linguist-vendored` in `.gitattributes` | A hit is only one piece of evidence; list the hits for checking per [invocation and scope resolution](scope.md#invocation-and-scope-resolution) |
| Symbol and document references | `rg -n -F -- '<symbol>' '<target-dir>'` | Search hits are leads for locating code; dynamic calls and external callers still need manual checking |
| Leftover markers | See the code block below | Judge per [2](dimensions/2-leftover-markers-and-suppressions.md); the number of hits is not the number of issues |
| Format checks | Go `gofmt -l '<file>'`; Python `ruff format --check '<file>'` or `black --check '<file>'`; JavaScript/TypeScript `prettier --check '<file>'`; Rust `cargo fmt --check`; C/C++ `clang-format --dry-run -Werror '<file>'`; Shell `shfmt -d '<file>'` | List differences only when the chosen format check applies; do not format automatically |
| Mapping documents to entry points | The project's existing tool, with explicit directory, package and document arguments | Check manually when there is no tool; a passing mapping does not prove that branches and contract descriptions are correct |
| Document links | `lychee --offline '<target-dir>'` or the project's existing link check | Offline mode checks only local files and anchors; checking external addresses needs the network, so handle that per authorization first |

Example search for leftover markers and suppression markers (fixed strings; multiple `-e` flags are combined as a union; add or remove patterns for the target languages):

```sh
rg -n -F \
  -e TODO -e FIXME -e XXX -e HACK \
  -e 'nolint' -e 'lint:ignore' -e 'nosec' \
  -e 'noqa' -e 'type: ignore' -e 'pylint: disable' -e 'pragma: no cover' \
  -e 'eslint-disable' -e '@ts-ignore' -e '@ts-expect-error' -e '@ts-nocheck' -e 'istanbul ignore' -e 'c8 ignore' \
  -e 'NOLINT' -e 'pragma GCC diagnostic' -e 'cppcheck-suppress' \
  -e '#[allow(' -e '#[expect(' \
  -e '@SuppressWarnings' -e '@Suppress(' -e 'pragma warning disable' \
  -e '@phpstan-ignore' -e '@psalm-suppress' -e 'phpcs:ignore' \
  -e 'rubocop:disable' -e ':nocov:' \
  -e 'shellcheck disable' -e 'swiftlint:disable' \
  -e 'NOSONAR' -e 'nosemgrep' -e 'gitleaks:allow' \
  -- '<target-dir>'
```

## On-demand verification

| Purpose | Example | Choosing and interpreting |
| --- | --- | --- |
| Ordinary correctness | Go `go test './<pkg>' -run '^TestName$'`; Python `pytest '<file>::<test>'`; JavaScript/TypeScript `npx vitest run '<file>' -t '<test-name>'` or `npx jest '<file>' -t '<test-name>'`; Rust `cargo test -p '<crate>' '<test>' -- --exact`; JVM `mvn -Dtest='<class>#<method>' test` or `gradle test --tests '<class>.<method>'`; .NET `dotnet test --filter 'FullyQualifiedName=<full-name>'`; C/C++ `ctest -R '^<test>$'` | Use test names confirmed in the source; for several tests, use a limited, exact selection; keep the tool's valid cache by default |
| Static analysis | Go `go vet './<pkg>'` or `staticcheck './<pkg>'`; Python `ruff check`, `pylint`; JavaScript/TypeScript `eslint`; C/C++ compiler `-Wall -Wextra`, `clang-tidy`, `cppcheck`, `scan-build`; Rust `cargo clippy`; JVM SpotBugs, Error Prone, PMD, Kotlin `detekt`; .NET build analyzers; PHP `phpstan`, `psalm`; Ruby `rubocop`; Shell `shellcheck`; cross-language `semgrep`, CodeQL | Pick tools as needed, and confirm the platform, build conditions, generated code and rule scope; no report does not mean no defect |
| Type checking | Python `mypy`, `pyright`; TypeScript `tsc --noEmit`; PHP `phpstan`, `psalm` | Covers only the parts with type information; runtime validation of external input still needs manual checking |
| Security rule scanning | Cross-language `semgrep`; Python `bandit`; Ruby `brakeman`; Go `gosec` | A rule hit is a lead; assess it per [27](dimensions/27-security-and-trust-boundaries.md) and the specialties; pulling rule sets over the network needs authorization first |
| Dead code leads | Go `deadcode`, the unused checks in `staticcheck`; Python `vulture`; JavaScript/TypeScript `knip`; Rust compiler unused warnings; at the dependency level, Rust `cargo udeps` | First confirm what that version can report; criteria in [1](dimensions/1-dead-code-and-reachability.md) |
| Duplication and complexity | Multi-language `jscpd` (duplication), `lizard` (complexity); Go `gocyclo` | Metrics are only leads; criteria in [3](dimensions/3-duplication.md) and [7](dimensions/7-complexity-and-maintainability.md) |
| Dependency graph and cycles | Go `go list -deps`, `go mod why`, `go mod graph`; JavaScript/TypeScript `madge --circular`; Python `pipdeptree`; Rust `cargo tree`; JVM `mvn dependency:tree`, `gradle dependencies` | Used for dependency direction in [5](dimensions/5-coupling-and-blast-radius.md) and for judging necessity in [35](dimensions/35-dependencies.md) |
| Dependency vulnerabilities | Go `govulncheck './<pkg>'`; Python `pip-audit`; JavaScript `npm audit`, `pnpm audit`; Rust `cargo audit`, `cargo deny`; JVM OWASP Dependency-Check; .NET `dotnet list package --vulnerable`; PHP `composer audit`; Ruby `bundler-audit`; cross-ecosystem `osv-scanner`, `trivy`, `grype` | Use tools the project has chosen and already installed; a tool querying online or refreshing its vulnerability data is a read-only network use allowed by default (see [the target project's hard boundaries](scope.md#the-target-projects-hard-boundaries)), while upgrading or installing the tool itself needs authorization first; assess results per [36](dimensions/36-supply-chain-and-artifact-integrity.md) |
| Dependency versions and security advisories | Go `go list -m -u all`; JavaScript `npm outdated`, `pnpm outdated`; Python `pip list --outdated`; PHP `composer outdated`; Ruby `bundle outdated`; .NET `dotnet list package --outdated`; without an audit tool, look up the package name and version in [OSV](https://osv.dev) or the [GitHub Advisory Database](https://github.com/advisories) | Read-only network lookups, allowed by default; "a newer version exists" does not mean "there is a security issue", so compare against advisories and fix versions and assess per [36](dimensions/36-supply-chain-and-artifact-integrity.md) |
| Dependency integrity | Go `go mod verify`; Rust `cargo build --locked`; Python `--require-hashes` at install time; npm `npm ci --ignore-scripts` in an isolated copy | Only shows that local dependencies match the lock file; commands that download or write the dependency directory run only in an isolated copy |
| Standalone build | Go `GOWORK=off go build -o '<tmp>/' './...'`; other ecosystems build in an isolated copy with workspace links and local overrides removed | Criteria in [38](dimensions/38-portability-and-build-context.md); do not change the source tree |
| Platform builds | Go `GOOS='<os>' GOARCH='<arch>' go build -o '<tmp>/artifact' './<pkg>'`; Rust `cargo build --target '<target-triple>'`; C/C++ cross toolchains. 32-bit targets can be used to check word size and atomic alignment | Only verifies the build for the given platform; it does not mean it runs correctly on that platform |
| API and ABI differences | Go `apidiff`, `gorelease`; Rust `cargo semver-checks`; JVM `japicmp`; C/C++ `abidiff` from libabigail; TypeScript `api-extractor` | Compares only two confirmed versions; interpret per [4.34](specialties/4.34-publishable-libraries-sdks-and-packages.md) |
| Coverage | Go `go test './<pkg>' -run '^TestName$' -coverprofile='<tmp>/cov.out'`, read with `go tool cover -func='<tmp>/cov.out'`; Python `coverage run -m pytest '<file>::<test>'`; JavaScript/TypeScript `c8`, `vitest --coverage`, `jest --coverage`; Rust `cargo llvm-cov`; JVM JaCoCo; .NET coverlet; C/C++ `gcov`, `llvm-cov` | Collect only when that measurement is authorized; interpret per [42](dimensions/42-coverage.md) |
| Concurrency detection | Go `go test './<pkg>' -race -run '^TestName$'`; C/C++/Rust ThreadSanitizer (`-fsanitize=thread`); Rust `loom` (needs matching test code); JVM `jcstress` | Select only the concurrent behavior that needs verification; limits of the conclusion in [18](dimensions/18-concurrency-and-memory-model.md) |
| Memory error detection | C/C++ `-fsanitize=address,undefined`, `valgrind`; Rust `cargo miri test`; Go with C code `go test -asan` | Select only the paths that need verification; no report does not mean no memory errors |
| Fuzzing | Go `go test './<pkg>' -run '^$' -fuzz '^FuzzName$' -fuzztime '<budget>'`; Python `atheris`; JavaScript `@jazzer.js/core`; C/C++ libFuzzer, AFL++; Rust `cargo fuzz`; JVM Jazzer; .NET SharpFuzz | Run only in an approved isolated copy; failing inputs may be written into the test data directory, so confirm ownership and the resource budget; not crashing does not prove safety |
| Property testing | Python `hypothesis`; JavaScript/TypeScript `fast-check`; Rust `proptest`; JVM `jqwik`; Go `testing/quick` | This is writing tests; whether to add them follows the project's test strategy |
| Diagnosing flaky issues | Go `go test './<pkg>' -run '^TestName$' -count '<count>'`; other frameworks use their own repeat options or plugins | Needs a failure analysis and a reason to repeat first; the count follows the authorized budget; keep the first failure and do not let a passing rerun overwrite it |
| Benchmarks and allocations | Go `go test './<pkg>' -run '^$' -bench '^BenchmarkName$' -benchmem`; Rust `cargo bench`; JVM JMH; Python `pyperf`, `pytest-benchmark` | Choose only when the user asks for performance measurement, and check the cost, input size and comparability conditions |
| Escape and allocation analysis | Go `go build -gcflags='-m' './<pkg>'`; other languages use an allocation profiler | Compiler reports only explain optimization decisions; costs on hot paths need evidence per the discipline in [31](dimensions/31-performance-memory-and-latency.md) |
| Secret scanning | `gitleaks detect --no-git --redact --source '<target-dir>'` | Use only tools and arguments that redact output and do not verify secrets over the network |
| Component inventory | `syft '<target-dir>'` | For comparison against the actual artifacts, see [36](dimensions/36-supply-chain-and-artifact-integrity.md) |
| Build, CI and deployment configuration | Shell `shellcheck`; Dockerfile `hadolint`; GitHub Actions `actionlint`, `zizmor`; infrastructure `checkov`, `trivy config`; Kubernetes `kube-linter` | Assess per [4.33](specialties/4.33-build-scripts-ci-and-infrastructure-as-code.md) |
| Smart contracts | Solidity: `slither`, `aderyn`, compiler warnings; symbolic execution `mythril`, `halmos`; fuzzing and invariant testing `forge test` (Foundry), `echidna`, `medusa`. Solana: `anchor test`. Move: `sui move test`, `aptos move test`, Move Prover | A rule hit is a lead; assess it per [4.38](specialties/4.38-smart-contracts-and-on-chain-interaction.md), [4.41](specialties/4.41-defi-economics-and-oracles.md) to [4.45](specialties/4.45-blockchain-nodes-consensus-and-protocol-implementations.md), [4.50](specialties/4.50-account-abstraction-and-smart-contract-wallets.md) and the matching language table; forking mainnet is only for local simulation, and no real transactions are sent |
| Non-EVM contracts | Solana fuzzing with Trident; Starknet static analysis `caracal`, tests `snforge test`; TON static analysis Misti; CosmWasm `cargo test` with `cw-multi-test` | A rule hit is a lead; criteria in the matching language table |
| Checking contract upgrades | Run `forge inspect '<contract>' storageLayout` on both the old and the new version and compare; `slither-check-upgradeability`; `validate` from OpenZeppelin Upgrades | Checks only the storage layout and how initialization is written; which initializations actually ran during the upgrade must be checked manually per [4.38](specialties/4.38-smart-contracts-and-on-chain-interaction.md) ("Upgrade procedure") |
| Checking the deployed version | `forge verify-bytecode '<address>' '<path>:<contract>'`; Solana `solana-verify get-program-hash` | Read-only queries; only proves that the on-chain code matches the given commit, not that the commit is safe |
| Checking transactions and signatures | `cast run '<tx-hash>'` (replays locally and prints the call trace); `cast decode-calldata`; for Safe multisig, `safe-tx-hashes-util` computes the transaction hash independently before signing | Replay happens only locally, and no transaction is sent; criteria in [4.43](specialties/4.43-wallets-signing-and-off-chain-components.md) |
| Zero-knowledge circuits | Detecting under-constrained circuits: for Circom, `circomspect` and Picus; halo2's `MockProver` only checks whether an honest witness satisfies the constraints, so write separate tests in which a modified witness must fail | No report from a tool does not mean the constraints are complete; criteria in [4.44](specialties/4.44-zero-knowledge-proofs-and-circuits.md) |
| On-chain reads | Foundry `cast code`, `cast storage`, `cast call`, `cast implementation`; verified source and contract read pages on block explorers | Read-only queries; record the chain ID and block number; criteria in [targets that are not source code: on-chain contract addresses](targets/on-chain-contract-addresses.md) |
| Compiled artifacts and binaries | `file`, `checksec`, `objdump`, `strings`; Linux `readelf -d` (RPATH, RUNPATH, BIND_NOW), `readelf -lW` (GNU_STACK), `readelf -n` (CET properties); Windows `winchecksec '<file>'`, `binskim analyze '<file>'`; macOS `otool -L`, `otool -l '<file>'` (`LC_RPATH`), `codesign -dv`, `codesign -d --entitlements - '<file>'`; embedded dependencies `go version -m '<file>'`, `govulncheck -mode=binary '<file>'`, `cargo audit bin '<file>'`; managed code `ilspycmd` (.NET), `jadx` (JVM and Android); decompilation Ghidra, radare2; component identification `syft` | Static inspection only; anything that needs running happens only in an isolated environment; criteria in [targets that are not source code: compiled artifacts and binaries](targets/compiled-artifacts-and-binaries.md) |
| Desktop installers | Windows signatures `osslsigncode verify '<file>'` or PowerShell `Get-AuthenticodeSignature '<file>'`; for MSI, `msiinfo export '<file>.msi' CustomAction`, `msidump`, `msiextract` from msitools; macOS `pkgutil --check-signature '<file>.pkg'`, `pkgutil --expand '<file>.pkg' '<tmp>/x'`, `spctl --assess -vv --type install '<file>.pkg'`, `xcrun stapler validate '<file>'`; deb `dpkg-deb -e '<file>.deb' '<tmp>/ctl'`; rpm `rpm -qp --scripts '<file>.rpm'`; Snap `unsquashfs -d '<tmp>/x' '<file>.snap'`, then read `meta/snap.yaml`; for Flatpak, read `finish-args` in the manifest, and for installed apps use `flatpak info --show-permissions '<app-id>'`; for Electron, `read --app '<app>'` from `@electron/fuses` and `asar extract '<app.asar>' '<tmp>/x'` from `@electron/asar` | Unpack only, do not install; do installing, repairing and running in an isolated virtual machine; AppImage's `--appimage-extract` runs the launcher bundled in the package, so use it only in an isolated environment; criteria in [targets that are not source code: desktop installers and desktop apps](targets/desktop-installers-and-desktop-apps.md) |
| Mobile app packages | `apktool`, `jadx`, `aapt dump badging`, `apksigner verify --print-certs -v '<file>.apk'`, MobSF; for iOS packages, `codesign -d --entitlements - '<app>'`, `plutil`, and `otool -l '<main-binary>'` to check `cryptid` in `LC_ENCRYPTION_INFO`; dynamic analysis Frida, objection | Do dynamic analysis only on isolated test devices; criteria in [targets that are not source code: mobile app packages](targets/mobile-app-packages.md) and [4.49](specialties/4.49-mobile-app-components-and-inter-process-communication.md) |
| Browser extensions | Unpack with `unzip -d '<tmp>/x' '<file>'` (a CRX has a signature header in front, so `unzip` warns about extra bytes but still extracts it); Firefox extensions `web-ext lint --source-dir '<dir>'` | lint checks only against Firefox's rules; permissions, messaging and remote code still need a manual read of `manifest.json` and the scripts; criteria in [targets that are not source code: browser extensions](targets/browser-extensions.md) |
| Firmware | `binwalk`, `unblob`, EMBA; emulation FirmAE; component versions `cve-bin-tool '<unpacked-dir>'` | Unpack in an isolated directory; `cve-bin-tool` downloads its vulnerability database on first run; criteria in [targets that are not source code: firmware and device images](targets/firmware-and-device-images.md) |
| IoT protocols | MQTT `nmap -p 1883,8883 --script mqtt-subscribe '<address>'`, `mosquitto_sub -h '<broker>' -t '#' -v -C '<count>'`; for BLE, `bluetoothctl`, nRF Sniffer with Wireshark, and `crackle` to analyze captures of legacy pairing; for Zigbee, KillerBee | Subscribing, pairing and replaying are all active operations, so do them only against approved devices and brokers; subscribing to `#` reads other people's data; radio transmission is subject to regulations; criteria in [targets that are not source code: device communication and IoT protocols](targets/device-communication-and-iot-protocols.md) |
| Container images | `docker history --no-trunc`, `dive`, `trivy image`, `grype`, `syft`; read the manifest and config without pulling image layers: `crane manifest`, `crane config`, `skopeo inspect 'docker://<image>'`; verify signatures with `cosign verify '<image>' --certificate-identity '<identity>' --certificate-oidc-issuer '<issuer>'` | Pulling an image downloads over the network, so handle it per authorization; signature verification without identity arguments cannot prove who signed; criteria in [targets that are not source code: container images](targets/container-images.md) |
| Published packages | npm `npm pack '<package>@<version>'`, `npm diff --diff='<package>@<old-version>' --diff='<package>@<new-version>'`, and `npm audit signatures` after `npm ci --ignore-scripts` in an isolated copy; for PyPI wheels, `pip download --no-deps --only-binary ':all:' -d '<tmp>' '<package>==<version>'`; for sdists, download directly from the address given by `https://pypi.org/pypi/<package>/<version>/json`; for digital attestations, `pypi-attestations verify pypi --repository '<repository-url>' 'pypi:<file-name>'`; Ruby `gem fetch '<package>' -v '<version>'`, then `gem unpack`; Rust: download from `https://static.crates.io/crates/<package>/<package>-<version>.crate`, then read `.cargo_vcs_info.json`; .NET `dotnet nuget verify '<package>.nupkg'`; JVM `jarsigner -verify -verbose '<file>.jar'`; compare with the source using `diff -r` or `diffoscope` | Downloading artifacts fetches data over the network, so handle it per authorization; `npm pack` does not run lifecycle scripts when it fetches from the registry; do not use `pip download` to fetch an sdist, because it runs the build backend, which means running code from the package; criteria in [targets that are not source code: published packages](targets/published-packages.md) |
| Signatures and provenance | For images, `cosign verify` with `--certificate-identity` and `--certificate-oidc-issuer`, and for attestations, `cosign verify-attestation --type '<type>'` with the same identity arguments; GitHub `gh attestation verify '<file>' --repo '<owner>/<repo>'`; SLSA `slsa-verifier verify-artifact '<file>' --provenance-path '<provenance>' --source-uri '<repo>' --source-tag '<tag>'`; in-toto `in-toto-verify` | You must state the expected signing identity and source origin; verification without identity arguments proves nothing; querying transparency logs is read-only network use; criteria in [targets that are not source code: signatures and provenance](targets/signatures-and-provenance.md) |
| SBOM and VEX | Format validation `cyclonedx validate --input-file '<file>' --fail-on-errors` (cyclonedx-cli); SPDX minimum elements `sbomcheck '<file>'` (ntia-conformance-checker); quality score `sbomqs score '<file>'`; vulnerabilities from the inventory `grype 'sbom:<file>'`, `trivy sbom '<file>'`, `osv-scanner scan -L '<file>'`; with VEX filtering `grype 'sbom:<file>' --vex '<vex-file>'` | Passing format validation does not mean the content is complete; list vulnerabilities filtered out by VEX separately and check the reason for each one; criteria in [targets that are not source code: dependency manifests, lock files and SBOMs](targets/dependency-manifests-lock-files-and-sboms.md) |
| Live services | Passive observation: `curl -sI`, `openssl s_client`, `dig`, Certificate Transparency log lookups. Active testing: `nmap`, `testssl.sh`, `nuclei`, ZAP active scan | Passive observation is done at a low rate by default; active testing needs written authorization; criteria in [targets that are not source code: live services](targets/live-services.md) |
| Frontend bundles and source maps | Recover source `sourcemapper -url '<map-url-or-file>' -output '<tmp>/src'`; deobfuscate `webcrack '<file>' -o '<tmp>/out'`; identify library versions `retire --path '<dir>'`; secrets `gitleaks detect --no-git --redact --source '<dir>'` | Fetching map files from a live site is passive access to public resources, so keep it low-rate; mark library versions identified by signatures as inferred; criteria in [targets that are not source code: frontend bundles and source maps](targets/frontend-bundles-and-source-maps.md) |
| API specification files | `spectral lint '<spec-file>'` (with the `@stoplight/spectral-owasp-ruleset` rule set), `vacuum lint '<spec-file>'`, `redocly lint '<spec-file>'` | The rules check only how the specification is written; whether authentication and authorization are actually enforced must be checked against the implementation or with authorized testing; criteria in [targets that are not source code: API specification files](targets/api-specification-files.md) |
| Domains, DNS and mail | `dig +short TXT '<domain>'`, `dig +short TXT '_dmarc.<domain>'`, `dig +short TXT '_mta-sts.<domain>'`, `dig +short CAA '<domain>'`; DNSSEC `delv '<domain>'`, `dnsviz probe -o '<tmp>/p.json' '<domain>'` then `dnsviz print -r '<tmp>/p.json'`; mail record summary `checkdmarc '<domain>'`; MTA-STS policy `curl -s 'https://mta-sts.<domain>/.well-known/mta-sts.txt'`; zone transfer `dig AXFR '<domain>' '@<name-server>'` | Public DNS queries and reading public policy files are passive observation; zone transfer and open resolver tests need authorization; criteria in [targets that are not source code: domains, DNS and mail configuration](targets/domains-dns-and-mail-configuration.md) |
| Hosts and operating systems | Linux `lynis audit system --logfile '<tmp>/lynis.log' --report-file '<tmp>/lynis-report.dat'`, OpenSCAP `oscap xccdf eval --profile '<profile>' --results '<tmp>/results.xml' '<datastream-file>'`, package integrity `rpm -Va`, `dpkg --verify`; Windows: `Invoke-HardeningKitty -Mode Audit -FileFindingList '<finding-list>'` from HardeningKitty; macOS: check scripts generated by mSCP; Docker hosts: `docker-bench-security` | A full check usually needs administrator privileges; Lynis writes to `/var/log` by default; docker-bench runs as a privileged container; choose baseline items by the host's purpose, and do not treat "does not meet the baseline" directly as a vulnerability; criteria in [targets that are not source code: hosts and operating systems](targets/hosts-and-operating-systems.md) |
| Cloud accounts | Prowler (besides the major clouds, it also supports `kubernetes`, `m365`, `github`), ScoutSuite, or read-only queries with the cloud provider's command-line tool | Use a read-only identity; ScoutSuite's last release was v5.14.0 (2024-05-10), so note when its rules were last updated; criteria in [targets that are not source code: cloud accounts and infrastructure state](targets/cloud-accounts-and-infrastructure-state.md) |
| Kubernetes clusters | Permissions `kubectl auth can-i --list` (add `--as` to check other subjects, which needs impersonation permission); baselines `kubescape scan framework '<framework>'`, `kube-bench`; resources and images `trivy k8s`; list only the names of secrets with `kubectl get secrets -A` | kube-bench and the kubescape host scan create privileged workloads in the cluster, which is a write operation, so get authorization first; do not read Secret values with `-o yaml`; record the control plane items of managed clusters as not applicable; criteria in [targets that are not source code: Kubernetes clusters](targets/kubernetes-clusters.md) |
| Helm and state files | Render with `helm template '<chart>' -f '<values-file>'` and pass the output to `kube-linter` and `checkov`; `helm verify '<chart-package>'` after `helm pull --prov`; read Terraform state directly with `jq '.resources[].instances[].attributes' '<state-file>'`, and scan it for secrets with `gitleaks detect --no-git --redact --source '<dir>'` | `terraform show -json` needs `terraform init` first, which downloads provider plugins; `terraform plan` and `refresh` use credentials to access the real environment, so they are not run by default; criteria in [targets that are not source code: configuration, deployment manifests and infrastructure code](targets/configuration-deployment-manifests-and-infrastructure-code.md) |
| Network device configuration | Batfish analyzes exported device configurations offline (reachability, shadowed and redundant ACL and firewall rules) | Analyzes only the exported configuration and does not connect to devices; changes made after the export are not visible; criteria in [targets that are not source code: network devices and firewall configuration](targets/network-devices-and-firewall-configuration.md) |
| SaaS tenants and code hosting | Microsoft 365 `Invoke-SCuBA` (ScubaGear); Google Workspace `scubagoggles gws` (ScubaGoggles); Prowler `prowler m365`, `prowler github`; GitHub and GitLab organizations `legitify analyze`; a single repository `scorecard --repo='<repo>'` | Most tools need an app registered in the tenant or read permissions granted, which is a configuration change, so get authorization first; use a read-only admin role; criteria in [targets that are not source code: SaaS tenants and third-party integrations](targets/saas-tenants-and-third-party-integrations.md) |
| Agent configurations | Hidden characters `rg -nP '[\x{200B}-\x{200F}\x{202A}-\x{202E}\x{2060}-\x{2064}\x{2066}-\x{2069}\x{E0000}-\x{E007F}]' -- '<dir>'`; secrets `gitleaks detect --no-git --redact --source '<dir>'`; dedicated scans `snyk-agent-scan inspect`, `snyk-agent-scan scan` | The dedicated scanner runs the server commands in the configuration, connects to remote servers, sends tool descriptions to the vendor's API and needs a vendor token, so use it only in an isolated environment after getting authorization; by default, only read the configuration statically; criteria in [targets that are not source code: agent configurations and MCP servers](targets/agent-configurations-and-mcp-servers.md) |
| WebAssembly modules | WABT's `wasm-objdump -x '<file>'`, `wasm2wat '<file>'`, `wasm-decompile '<file>'`; `wasm-tools validate '<file>'`, `wasm-tools print '<file>'` | Static inspection only; anything that needs running goes in an isolated host; criteria in [targets that are not source code: WebAssembly modules](targets/webassembly-modules.md) |
| Office documents and PDFs | oletools' `oleid`, `olevba`, `mraptor`, `msodde`, `oleobj`, `rtfobj`; for PDF, `pdfid.py`, `pdf-parser.py`, `qpdf --check`, and expand with `qpdf --qdf --object-streams=disable '<file>' '<tmp>/x.pdf'` before reading; metadata `exiftool '<file>'` | Do not open them in office software or a reader, and do not enable macros; do not upload them to online scanning services; criteria in [targets that are not source code: office documents and PDFs](targets/office-documents-and-pdfs.md) |
| Logs and packet captures | `capinfos -H '<file>'` (summary and time range), `tshark -r '<file>' -q -z conv,tcp`, `zeek -r '<file>'` run inside the `<tmp>` directory (logs are written to the current directory), `suricata -r '<file>' -l '<tmp>'` | Analyze a copy; packet captures and HAR files contain third parties' personal data and session credentials, so handle them as sensitive data; criteria in [targets that are not source code: logs, traffic and runtime data](targets/logs-traffic-and-runtime-data.md) |

After running a command, keep the original exit status and the necessary output; do not just count the matched failure lines. Record zero matches, tool incompatibility, missing data or environment failures per [Part V](report.md#part-v-evidence-levels-and-report-format). Secret scans output only redacted locations; never carry raw credential values into the report or upload them to external services.

Tool results and manual analysis complement each other. A tool finding no issues only shows that the tool reported nothing under the recorded rules, paths and inputs. Data flows, business authorization, invariants and attack chains are still checked object by object per [Part II](questions.md).
