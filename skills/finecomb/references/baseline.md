# Part I: Establish the factual baseline

1. **Set the scope and its basis**: First read the applicable rules, then the overview documents, flow contracts and test matrix relevant to this review. Per [invocation and scope resolution](scope.md#invocation-and-scope-resolution), list the target, the exclusions and the suspected items, plus the review time, a verifiable identity of the source or artifacts, the actual configuration, the platform and the build conditions. Judge missing documents that the project requires per [43](dimensions/43-documentation-consistency.md).
2. **Build the language and build inventory**:
   - Identify the language of each path by extension, file header and build manifest. Also find languages embedded in strings or configuration (SQL, regular expressions, Shell, templates, HTML, expressions).
   - List the build systems and manifest files (such as `go.mod`, `package.json`, `pyproject.toml`, `requirements*.txt`, `Cargo.toml`, `CMakeLists.txt`, `Makefile`, `pom.xml`, `build.gradle`, `*.csproj`, `composer.json`, `Gemfile`), and the declared language and runtime versions.
   - Find the entry forms of each language: executable entry points, command-line scripts, package exports, route registration, plugin registration, code run at import time.
   - Find cross-language boundaries (see [4.32](specialties/4.32-cross-language-boundaries-and-native-extensions.md)).
   - For each language, pick the matching table in [Appendix A](languages.md).
   - When the target is not source code, build an artifact inventory per [targets that are not source code](targets/index.md) instead: the form of each artifact, its digest and version, the evidence you can get, and the parts you cannot see.
3. **Build the object inventory**: Read the implementation per [scaling to the request](scope.md#scaling-to-the-request). List file responsibilities, public entry points, externally reachable routes and admin entry points, background flows and automatic behaviors (threads, coroutines, timers, finalizers, code run at import or load, signal handlers), shared state, invariants and side effects. For dynamic registration, reflection, plugins and conditional compilation, reachability cannot be judged by counting text references alone. Handle content that is not hand-written per the project's maintenance boundaries.
4. **Confirm upstream and downstream**: List the dependencies, callers, persisted data and external systems actually used. Distinguish "present in source", "included in the build", "enabled in deployment" and "reachable from outside". Without deployment evidence, keep the condition attached; do not treat the default configuration as a production fact.
5. **Determine the target type**: For security-related targets, set the assets, attacker capabilities and trust boundaries per [the next section](#threat-model-and-attack-chains), then pick [specialties](specialties/index.md) per [mapping known attack mechanisms](#mapping-known-attack-mechanisms), and check [historical vulnerability patterns](history/index.md) to pick out the relevant groups.
6. **Check existing evidence and choose only the execution you need**: First check whether reusable records are still valid. Choose actions that need execution per [execution boundaries during review](scope.md#execution-boundaries-during-review) and the project's test strategy; [Part VII](tools.md) is only for picking tools. Issues that a read-only review can settle statically may be reported directly. For candidate issues that need reproduction, choose only an authorized smallest entry point.

## Threat model and attack chains

For security-related targets, record the facts below. Write missing information as assumptions or unverified items. Do not invent preconditions to close an attack chain.

| Subject | What to confirm |
| --- | --- |
| Assets and security goals | Data, credentials, permissions, funds, compute resources and service availability; who owns them, and which reads, changes and interruptions must not happen |
| Attacker capabilities | What each of these can do: anonymous users, ordinary users, other tenants, low-privilege operators, a compromised client or dependency, **other users or low-privilege processes on the same machine**, **web pages in a local browser** (which can send requests via localhost and DNS rebinding), external contributors whose commits trigger CI, upstream dependency or plugin authors; whether credentials, network position and user interaction are preconditions |
| Controllable inputs | Requests, messages, files, stored records, configuration, environment, build inputs, source code or rules handed to an interpreter; which fields can be controlled directly and which can only be influenced indirectly |
| Trust boundaries | Browser to service, between tenants, service to data layer, build to release, data to interpreter, script to host, local low-privilege process to high-privilege service, one language to another; at each crossing, who verifies identity, content and permissions |
| Attack surface and blocking points | The entry points actually enabled and the controls at each layer; whether proxies, gateways, permission checks, sandboxes and isolation policies can be bypassed, and whether the configuration they rely on has been verified |
| Final impact | The full path from controlled input to a violated security goal, the number of attempts and the cost needed, the scope of impact, persistence and recovery conditions |

Trace along "input source → decoding and normalization → validation and authorization → sensitive operation → state or external result". Focus on whether the object that was validated is the same object that is finally used, and whether data is still protected after it passes through storage, queues, callbacks or a second parse. A valid signature only shows that the signed content passed verification. It does not mean the content follows business rules or that the caller is allowed to perform the action.

Several candidate issues can form an attack chain, but every step needs evidence that it is reachable. If the chain relies on an administrator's active cooperation, a key not proven to be obtainable, or a deployment configuration that does not exist, do not report it as directly exploitable by an ordinary user. For source code review, the data flow and threat analysis methods of [OWASP Secure Code Review](https://cheatsheetseries.owasp.org/cheatsheets/Secure_Code_Review_Cheat_Sheet.html) can serve as a reference.

When a mapping to an external standard is needed, record the version, the items and the reason for applicability actually used. For example, [OWASP ASVS](https://owasp.org/www-project-application-security-verification-standard/) uses versioned item identifiers. Completing this checklist does not mean certification against any standard.

## Mapping known attack mechanisms

This table maps features of an entry point to where to check. It does not repeat the criteria, and it does not require reporting the same issue again. Finer-grained patterns, cases and search methods, drawn from real historical vulnerabilities, are in [historical vulnerability patterns](history/index.md).

| If the target has | How it has been broken before | Go to |
| --- | --- | --- |
| Connects to an address taken from external data (parameters, stored records, configuration, redirects, webhooks, callback URLs) | Outbound requests hit the internal network / cloud metadata / the file protocol; DNS rebinding bypasses allowlists | [4.21](specialties/4.21-outbound-requests-and-server-side-request-forgery.md) |
| Starts subprocesses or interpreters, uses eval, deserializes bytes into object graphs, loads plugins | Code runs even without "splicing into a command": inherited environment variables, gadgets, decoders that read local files or reach the network | [4.22](specialties/4.22-subprocesses-dynamic-execution-and-decoder-side-effects.md) |
| A parser fetches external entities or references | Reads local files, makes outbound requests | [4.3](specialties/4.3-decoding-untrusted-external-data.md) |
| Cookies / session credentials are sent automatically with browser requests, and the entry point changes state | The user is tricked into sending the request from another site | [4.14](specialties/4.14-server-request-handling-and-middleware.md), [4.18](specialties/4.18-user-interfaces-and-accessibility.md) |
| Writes external input into a redirect target (`Location`, return after login, generic redirects) | Sends people to a phishing page, or sends tokens somewhere else | [4.18](specialties/4.18-user-interfaces-and-accessibility.md), [4.5](specialties/4.5-authentication-sessions-and-tokens.md) |
| Parses HTTP itself, or sits between two HTTP implementations (a proxy, a CDN in front) | Disagreement on length and chunked semantics makes the front and back see different requests; unvalidated headers end up in cache keys | [4.14](specialties/4.14-server-request-handling-and-middleware.md), [4.2](specialties/4.2-protocols-and-frame-parsing.md) |
| Uses untrusted strings as hash table / dictionary keys | Collisions let a single request max out the CPU | [20](dimensions/20-resource-bounds-and-backpressure.md), [27](dimensions/27-security-and-trust-boundaries.md) |
| Internal package names also resolve on public registries | Someone registers the same name publicly, and the build pulls the fake package | [36](dimensions/36-supply-chain-and-artifact-integrity.md) |
| Logs / metrics / tracing interpret lookups or expressions inside messages | Log content turns into outbound requests or code execution | [4.13](specialties/4.13-logs-metrics-and-tracing.md), [4.22](specialties/4.22-subprocesses-dynamic-execution-and-decoder-side-effects.md) |
| Binds request objects directly to internal models, or recursively merges external objects | Writes unauthorized fields, or affects other objects through prototype pollution | [28](dimensions/28-authorization-and-access-control.md), [4.3](specialties/4.3-decoding-untrusted-external-data.md) |
| Updates business state after receiving an external callback | A valid signature is reused across tenants or operations, or old events take effect again and again | [4.24](specialties/4.24-webhooks-and-external-events.md) |
| Writes shared data based on a lease or a leader election result | The old holder recovers and keeps writing, overwriting the new holder's results | [4.25](specialties/4.25-distributed-coordination-and-leases.md) |
| An LLM reads external content and then calls tools | The external content rewrites the task, then uses the tools' permissions to read or modify resources it should not touch; when the output is rendered, it automatically requests external addresses and carries data out; the agent rewrites its own approval settings | [4.26](specialties/4.26-llms-and-tool-calling.md) |
| Runs source code, expressions, templates, queries or rules supplied from outside | A script gets a host object and escapes the sandbox; built-in functions bypass metering, and one line of code exhausts memory or CPU | [4.27](specialties/4.27-interpreters-compilers-and-virtual-machines.md), [20](dimensions/20-resource-bounds-and-backpressure.md) |
| Forwards other people's traffic, or takes over system routing or name resolution | Its own traffic is captured by itself and forms a loop; name resolution or IPv6 bypasses the tunnel and leaks; it is used as an open proxy or a reflection amplifier | [4.28](specialties/4.28-tunnels-proxies-and-the-network-data-plane.md) |
| Allows, blocks, routes or rate-limits by a rule table | Suffix or wildcard boundaries are wrong; a rule is shadowed by an earlier rule; no normalization before matching | [4.29](specialties/4.29-rule-and-policy-matching.md) |
| Calls system interfaces directly, or holds raw descriptors or handles | After a descriptor number is reused, someone else's descriptor is closed by mistake; a call interrupted by a signal is treated as a failure; lengths returned by the kernel are not validated | [4.30](specialties/4.30-os-interfaces-system-calls-and-descriptors.md) |
| Generates code from a schema, IDL or template | Names in the schema are injected into the generated code; old artifacts remain after their input is deleted | [4.31](specialties/4.31-code-generators-and-build-time-tools.md) |
| Two languages call each other, share memory or share data definitions | Integer widths and null values do not match; exceptions cross a boundary that does not support them; the two sides disagree on who owns memory | [4.32](specialties/4.32-cross-language-boundaries-and-native-extensions.md) |
| CI splices event fields into scripts, or runs external commits in a privileged context | External contributors get release credentials or write access | [4.33](specialties/4.33-build-scripts-ci-and-infrastructure-as-code.md), [36](dimensions/36-supply-chain-and-artifact-integrity.md) |
| Publishes packages for others to install or link | Published content carries secrets; code runs at install time; breaking changes are not reflected in the version number | [4.34](specialties/4.34-publishable-libraries-sdks-and-packages.md) |
| The client auto-updates, or can be launched by other apps or web pages | Update packages are replaced or downgraded; exported components or custom protocols are invoked by other apps or web pages | [4.35](specialties/4.35-client-apps-extensions-and-auto-update.md) |
| A high-privilege resident service opens a control port on the local machine | A local low-privilege process or a web page in a browser drives it to perform privileged operations | [4.35](specialties/4.35-client-apps-extensions-and-auto-update.md), [28](dimensions/28-authorization-and-access-control.md), [34](dimensions/34-runtime-environment-and-deployment-contract.md) |
| Runs with higher privileges than its caller (services, setuid programs, privileged helpers, installers) and handles requests or files that other local users can reach | Identifying the caller by process ID lets it be impersonated; file operations in user-writable directories are redirected to system files through links; libraries are loaded from writable paths | [4.47](specialties/4.47-local-privileged-components-and-local-privilege-escalation.md) |
| Kernel drivers, hypervisors or device emulation | Ordinary users get arbitrary memory reads and writes through control codes; mappings stay usable after they are freed; the guest rewrites descriptors that have already been validated | [4.48](specialties/4.48-kernel-drivers-device-emulation-and-virtualization.md) |
| Runs in firmware, drivers or interrupt context | Interrupt reentry corrupts shared data; power loss while writing flash bricks the device; the device cannot boot after an interrupted update | [4.36](specialties/4.36-embedded-firmware-and-real-time-constraints.md) |
| Handles downloaded or foreign files (unpacking, converting, saving as, opening) | The origin mark is lost after unpacking or copying, and the foreign file is run as a trusted local file | [4.35](specialties/4.35-client-apps-extensions-and-auto-update.md) ("Download origin marks"), [4.8](specialties/4.8-file-systems-and-paths.md) |
| Logs in with OAuth, OpenID Connect or SAML, accepts NTLM or Kerberos authentication, or issues certificates | A loosened redirect URI sends the authorization code away; the signature is verified on one assertion but a different one is read; coerced authentication is relayed; someone else's credentials are issued based on a name that can be changed | [4.46](specialties/4.46-federated-identity-and-single-sign-on.md), [4.5](specialties/4.5-authentication-sessions-and-tokens.md) |
| Android or iOS app source code | Exported components are called by other apps; received Intents are forwarded; deep links make a WebView load arbitrary pages | [4.49](specialties/4.49-mobile-app-components-and-inter-process-communication.md) |
| Reruns batch jobs, trains models or loads model files | Reruns write duplicates; training and test data leak into each other; loading a model file runs code | [4.37](specialties/4.37-data-processing-batch-jobs-and-machine-learning.md), [4.22](specialties/4.22-subprocesses-dynamic-execution-and-decoder-side-effects.md) |
| On-chain contracts hold or transfer assets | Reentrancy, price manipulation, front-running, storage layout conflicts after an upgrade; an upgrade misses initialization; developer addresses keep privileges | [4.38](specialties/4.38-smart-contracts-and-on-chain-interaction.md); by chain, pick [Solidity and Vyper](lang-solidity.md), [Solana](lang-solana.md), [Move](lang-move.md), [CosmWasm](lang-cosmwasm.md), [Cairo](lang-cairo.md), [TON](lang-ton.md) |
| Contracts that run arbitrary calls on the caller's behalf, such as routers, aggregators and batch executors | Approvals that users gave the contract are drained by someone else through an arbitrary call; anyone can call the callback entry point directly | [4.38](specialties/4.38-smart-contracts-and-on-chain-interaction.md) ("Arbitrary calls"), [Solidity and Vyper](lang-solidity.md) ("Callback origin") |
| On-chain financial protocols such as lending, exchanges and stablecoins | Flash loans manipulate prices or voting power; share rounding lets the first depositor take what later depositors put in; the empty pool of a newly launched market is manipulated; liquidation fails | [4.41](specialties/4.41-defi-economics-and-oracles.md) |
| Moves assets or messages between chains | Default values make any message count as verified; the keys of a few validators are taken over; forged system accounts; each path is configured with only one verifier | [4.42](specialties/4.42-cross-chain-bridges-and-messaging.md) |
| Contract wallets, account abstraction, EIP-7702 delegation | The validation function can be called directly; the paymaster is drained; delegation initialization is front-run; delegation authorizations that are valid across chains | [4.50](specialties/4.50-account-abstraction-and-smart-contract-wallets.md) |
| Manages private keys, coordinates multisig or provides a dApp frontend | The signing device does not show what is actually being signed (blind signing); a tampered frontend tricks users into granting approvals | [4.43](specialties/4.43-wallets-signing-and-off-chain-components.md) |
| Uses zero-knowledge proofs, private transactions or custom cryptographic protocols | The circuit assigns values without constraining them, so a malicious prover forges proofs; the challenge hash leaves out items; one secret derives several "unique" identifiers | [4.44](specialties/4.44-zero-knowledge-proofs-and-circuits.md), [zero-knowledge circuits](lang-zk.md), [4.4](specialties/4.4-cryptography-and-credentials.md) |
| Implements blockchain nodes, consensus or transaction verification | An optimized path skips the double-spend check; an amount overflow mints coins out of nothing; nodes disagree on results, which causes a fork | [4.45](specialties/4.45-blockchain-nodes-consensus-and-protocol-implementations.md) |
| Handles payments, refunds, balances or bills | Retries cause double charges; concurrent overdrafts; total refunds exceed the original payment | [4.39](specialties/4.39-payments-accounting-and-billing.md) |
| Sends SMS, email or push notifications to the outside | Attackers trigger mass SMS and run up messaging charges; it is used as a spam relay | [4.40](specialties/4.40-notifications-and-outbound-messages.md) |
| The target has only artifacts, addresses or documents, and no source code | Version inference replaces evidence; what cannot be seen is written as "none found" | [targets that are not source code](targets/index.md) |
| Publishes packages or browser extensions for others to install | After the publishing account is stolen, a malicious version is pushed; extension permissions are too broad and are borrowed by web pages | [targets that are not source code: published packages](targets/published-packages.md), [browser extensions](targets/browser-extensions.md), [36](dimensions/36-supply-chain-and-artifact-integrity.md) |
| Third-party integrations that connect to SaaS through OAuth or long-lived tokens | After the integrator is compromised, its tokens are used to export customer data in bulk | [targets that are not source code: SaaS tenants and third-party integrations](targets/saas-tenants-and-third-party-integrations.md), [4.46](specialties/4.46-federated-identity-and-single-sign-on.md) |
| An agent starts external tool servers from its configuration | A server whose version is not pinned gets replaced; tool descriptions are poisoned; a local debugging service is driven by a web page through DNS rebinding | [targets that are not source code: agent configurations and MCP servers](targets/agent-configurations-and-mcp-servers.md), [4.26](specialties/4.26-llms-and-tool-calling.md) |
| Runs resident for a long time, unattended | Hot loops while idle; counter wraparound; certificates expire while running; state goes stale after sleep and wake | [45](dimensions/45-long-running-and-resident-processes.md) |
| Features that send messages for users, give out free resources, or move money, credits or rewards | Invitations and shares used for spam and phishing; free tiers used for mining or file hosting; referral and coupon fraud | [46](dimensions/46-abuse-and-fraud-resistance.md) |
| Code inside, or talking to, an enclave or trusted execution environment | Pointers and results supplied by the host are trusted; sealed data is rolled back; attestation is not bound to the channel | [4.51](specialties/4.51-trusted-execution-environments-and-enclaves.md) |
| Monitors or controls physical processes (controllers, SCADA, robots, vehicles, medical devices) | Control protocols without authentication are reachable from business networks; software bypasses safety interlocks | [4.52](specialties/4.52-industrial-control-and-cyber-physical-safety.md) |
| Search, autocomplete, recommendations, or retrieval for LLMs | Results and snippets ignore document permissions; the index keeps serving content after access is removed | [4.53](specialties/4.53-search-indexing-and-retrieval.md) |
| Sync, real-time collaboration or offline clients | Operations accepted because they came through sync; offline changes applied after access was revoked; deleted items come back | [4.54](specialties/4.54-sync-collaboration-and-offline-clients.md) |
| Serverless or edge functions | Event fields trusted; one broad role for every function; warm instances carry data between callers | [4.55](specialties/4.55-serverless-and-edge-functions.md) |
| Hosts plugins, extensions, marketplace apps or third-party OAuth apps | Plugins run with the host's full privileges; declared permissions are not enforced at run time; updates or ownership transfers push new code without review; tokens and hooks outlive uninstall | [4.56](specialties/4.56-plugin-extension-and-app-platforms.md) |
| Workflow automation, integration or low-code platforms, or rule and expression engines that users configure | Anyone who can edit a workflow runs code through expressions or code nodes; stored integration credentials are used by other users; public triggers reach internal steps | [4.57](specialties/4.57-workflow-automation-and-low-code-platforms.md) |
| Serves many tenants from shared infrastructure, or hosts user sites, uploads or custom domains | Tenant context is lost in background jobs, caches or exports; stale custom domains are claimed by others; user HTML is served from the application's origin; share links are guessable or never expire | [4.58](specialties/4.58-multi-tenant-platforms-and-hosting-of-user-content.md) |
| Remote desktop or support tools, RMM, MDM or fleet agents that take commands from a server | Setup endpoints still reachable after installation give admin access; agents accept unsigned commands or updates; one console account runs code on every managed device | [4.59](specialties/4.59-remote-access-and-device-management.md) |
| Antivirus, EDR, scanners, WAFs, IDS, sandboxes, SIEM or mail security gateways | Privileged parsers fail on hostile files; the scanner runs code from the target it scans; content parsed differently from the real consumer passes; findings inject into the analyst's console | [4.60](specialties/4.60-security-tools-that-process-untrusted-content.md) |
| Runs a CA, an ACME server or client, internal PKI or certificate automation | Validation is passed through DNS, routing or redirect tricks; requesters get certificates for names they do not own; CA keys lack hardware protection; issued certificates miss constraints | [4.61](specialties/4.61-certificate-authorities-and-pki-operations.md) |
| Authoritative DNS servers, recursive resolvers, DNS forwarders or dynamic DNS | Predictable IDs or loose answer checks let the cache be poisoned; zone updates are accepted by source address alone; one query triggers unbounded recursion or validation work | [4.62](specialties/4.62-dns-servers-and-resolvers.md) |
| Telecom signaling, VoIP or SIP trunks, SMS gateways, or number and SIM provisioning | Signaling is trusted because of network position; SMS codes are the only proof of identity; numbers are ported or SIMs swapped with weak checks; trunks are abused for toll fraud | [4.63](specialties/4.63-telecom-signaling-and-sms.md) |
| End-to-end encrypted messaging, including groups, linked devices or encrypted backups | The server silently adds a device or group member; key changes are accepted without a clear warning; backups, attachments or previews sit outside the encryption | [4.64](specialties/4.64-end-to-end-encrypted-messaging.md) |
| Order entry, matching engines, brokers or exchanges | Pre-trade checks are skipped on one order path; cancel, amend and fill races release or spend funds twice; rounding favors the trader across many small orders; hot funds leave through weak withdrawal limits | [4.65](specialties/4.65-trading-exchanges-and-order-matching.md) |
| Hardware designs: RTL or HDL code, SoC integration, security IP or boot ROM | Debug and test access stays open in production; lock bits change after they are set; bus security attributes are lost or not checked; boot ROM parses images without full checks | [4.66](specialties/4.66-hardware-designs-and-hardware-security.md) |
| Location or "nearby" features, presence, read receipts, social graphs or audience settings | Exact position is recovered from repeated distance queries; visibility settings apply in one view but not in APIs, search or exports; blocked users reach the person through another feature | [4.67](specialties/4.67-location-presence-and-visibility-of-personal-data.md) |
| Container runtimes, sandboxed runtimes or tools that unpack images | Host descriptors or directories leak into the container; mounts and image layers follow links out of the root; restrictions are requested but not applied on some paths such as exec | [4.68](specialties/4.68-container-runtimes-and-isolation.md) |
| Hosts packages or artifacts, or runs a registry mirror or caching proxy | Deleted names are registered again by someone else; loose trusted publishing rules accept any workflow; archive contents differ from the displayed metadata; a proxy serves a public package in place of a private one | [4.69](specialties/4.69-package-registries-and-artifact-repositories.md) |
| Version control clients or servers, code hosting or code review tools | Repository content runs commands on clone or checkout; names that collide on case-insensitive file systems write into metadata; pull requests from forks run with the base repository's secrets; branch protection is bypassed through another path | [4.70](specialties/4.70-code-hosting-and-version-control.md) |
| Game servers, matchmaking, or in-game economies and stores | The server trusts results the client reports; retries and disconnects duplicate items or currency; trades are not atomic; the server relies on the client's anti-cheat verdict | [4.71](specialties/4.71-online-games-and-multiplayer-servers.md) |
| Streams media, signs media URLs or runs a DRM license server | Signed URLs whose signature does not cover the path, expiry or client; license keys handed out without device, entitlement or concurrency checks; region or subscription checks done only in the player | [4.72](specialties/4.72-media-streaming-drm-and-content-delivery.md) |
| Takes backups, snapshots or dumps, or runs replication for disaster recovery | Backups protected more weakly than the source; restores allowed to more people or to any location; backups that a production admin can delete or encrypt; restores bringing back revoked access | [4.73](specialties/4.73-backup-archive-and-disaster-recovery.md) |
| Runs or hosts notebooks, REPL servers or interactive consoles | Servers and kernels reachable without a token or from any web origin; kernels or workspaces shared between users; notebook outputs running scripts in the server's origin | [4.74](specialties/4.74-notebooks-and-interactive-computing.md) |
| Biometric login, liveness checks, document verification or identity verification vendors | Results decided on the client and trusted by the server; vendor results taken from unsigned callbacks or not tied to the user and attempt; weaker fallback paths giving the same access | [4.75](specialties/4.75-biometrics-and-identity-verification.md) |
| Stores or exchanges health records, medical images or patient data | Patient scope and consent checked in the app but not on every API, search or export; exchange and imaging files parsed without limits; interface listeners accepting messages without authentication | [4.76](specialties/4.76-health-data-and-medical-interoperability.md) |
| Runs a mail server, webmail, or code that parses or acts on incoming mail | Relaying for unauthenticated clients; end-of-data and line-ending differences between hops splitting one message into two; sender checks done on a different identity than the one shown; webmail rendering untrusted HTML | [4.77](specialties/4.77-mail-servers-and-mail-handling.md) |
| Splits privilege between sandboxed processes and a privileged broker or helper | The broker trusts IDs, paths or claimed identity in messages from the sandbox; handles are passed with too many rights; the sandbox is missing or silently disabled on some platforms | [4.78](specialties/4.78-sandboxes-and-broker-processes.md) |
